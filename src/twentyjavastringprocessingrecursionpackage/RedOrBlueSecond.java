package twentyjavastringprocessingrecursionpackage;
import java.util.ArrayList;
import java.util.Arrays;
public class RedOrBlueSecond {
    /*public static void main(String[] args) {
        System.out.println(plus3("redxx", 3));
    }
    public static String plus3(String s, int k) {
        String result = "";​
        for (int i = 0; i < s.length(); i++) {
            char c = s.charAt(i);
            if (Character.isUpperCase(c)) result += (char) (((c - 65 + k) % 26) + 65);
            if (Character.isLowerCase(c)) result += (char) (((c - 97 + k) % 26) + 97);
        }
        return result;
    }
    }*/
}

